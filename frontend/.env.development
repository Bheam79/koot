# When running the Vite dev server inside the dev container, API requests are
# proxied through Vite to the backend at localhost:5024.  An empty VITE_API_URL
# causes axios to use relative paths (/api/...) so the Vite proxy can intercept
# them.  See vite.config.ts for the proxy rules.
VITE_API_URL=
